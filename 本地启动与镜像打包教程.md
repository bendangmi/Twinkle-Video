# Twinkle-Video 本地启动与镜像打包教程

## 一、统一端口和数据库

| 用途 | 地址 |
| --- | --- |
| 单端口全栈开发 | `http://127.0.0.1:46511` |
| 分离开发前端 | `http://127.0.0.1:46511` |
| 分离开发后端 | `http://127.0.0.1:46512` |
| Docker 一体化服务 | `http://127.0.0.1:46511` |

页面、Next.js Route Handler 和 Worker 位于同一 `web` 项目。数据库统一使用 `E:\docker\pgsql`：

- PostgreSQL：`127.0.0.1:5432`
- 数据库：`vozeb_pro`
- 账号密码：读取 `E:\docker\pgsql\.env`
- Redis、MySQL、RabbitMQ：本项目当前不需要

## 二、启动 PostgreSQL

```powershell
docker compose --env-file E:\docker\pgsql\.env -f E:\docker\pgsql\docker-compose.yaml up -d
docker ps --filter name=pgsql
```

检查项目数据库：

```powershell
docker exec pgsql psql -U pguser -d postgres -Atc "SELECT datname FROM pg_database WHERE datname='vozeb_pro';"
```

如果没有返回 `vozeb_pro`，创建一次：

```powershell
docker exec pgsql psql -U pguser -d postgres -c "CREATE DATABASE vozeb_pro OWNER pguser;"
```

本地源码连接信息已写入 `web\.env.local`；Docker 连接信息已写入根目录 `.env`，容器通过 `host.docker.internal` 访问宿主机数据库。

## 三、安装依赖

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Video
corepack enable
pnpm --dir web install --frozen-lockfile
```

## 四、推荐：单终端全栈启动

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Video
pnpm dev
```

默认访问：

- 前端：<http://127.0.0.1:46511>
- 安装页：<http://127.0.0.1:46511/install>
- 健康检查：<http://127.0.0.1:46511/api/health/live>

## 五、终端分别启动前后端

终端一，启动后端 Route Handler 和 Worker：

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Video
$env:FRONTEND_PORT = "46511"
$env:BACKEND_PORT = "46512"
pnpm dev:backend
```

终端二，启动前端：

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Video
$env:FRONTEND_PORT = "46511"
$env:BACKEND_PORT = "46512"
pnpm dev:frontend
```

检查：

```powershell
Invoke-WebRequest http://127.0.0.1:46512/api/health/live
Invoke-WebRequest http://127.0.0.1:46511/api/health/live
```

停止服务：在两个终端分别按 `Ctrl+C`。

## 六、构建最新 Docker 镜像

```powershell
Set-Location C:\Users\14740\Desktop\Twinkle-Project\Twinkle-Video
docker build --pull -t twinkle-video:latest .
```

建议增加不可变标签：

```powershell
$version = Get-Date -Format 'yyyyMMdd-HHmm'
docker tag twinkle-video:latest "twinkle-video:$version"
```

## 七、使用 E:\docker 数据库运行镜像

必须使用外部数据库 Compose，不要使用会再创建 PostgreSQL 的 `docker-compose.local.yml`：

```powershell
docker compose -f docker-compose.external-db.yml up -d
docker compose -f docker-compose.external-db.yml ps
docker compose -f docker-compose.external-db.yml logs -f app generation-worker
```

访问：<http://127.0.0.1:46511>

停止服务：

```powershell
docker compose -f docker-compose.external-db.yml down
```

不要添加 `-v`，否则会删除应用数据卷。生产发布前必须更换 `.env` 中的安装令牌、维护令牌、Worker 令牌和加密密钥，并将 `NEXT_PUBLIC_SITE_URL` 改为实际 HTTPS 域名。

## 八、导出、导入和推送镜像

```powershell
docker save -o twinkle-video-latest.tar twinkle-video:latest
docker load -i twinkle-video-latest.tar
docker tag twinkle-video:latest <你的仓库>/twinkle-video:latest
docker push <你的仓库>/twinkle-video:latest
```

镜像不包含 PostgreSQL 和 Docker 数据卷，上线迁移时必须单独备份数据库与应用数据卷。
